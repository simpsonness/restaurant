<h1>Your Plan A Tables</h1>
<% @tables.each do |table| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <%= link_to table.title, table_path(table.id) %>
      </h4>
    </div>
  </div>
<% end %>
